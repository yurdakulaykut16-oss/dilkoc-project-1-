import type { LanguageConfig } from "../../engine/types";

export const ruConfig: LanguageConfig = {
  code: "ru",
  nativeName: "Русский",
  script: "cyrillic",
};

// Vocabulary, grammar, and curriculum data for Russian will be added in later phases.
